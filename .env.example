# Copy to .env at the repo root (never commit .env).
# Docker Compose / Makefile load this file automatically.

# Carto basemap API key for the provincial map tiles
VITE_CARTO_API_KEY=
AEMET_API_KEY=
